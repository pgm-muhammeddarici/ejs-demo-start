<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%=title %> </title>
</head>
<body>
    <h1><%= title %></h1>
    <hr>
    <p>
        <%= content %>
    </p>
    <hr>
        <% if(team) { %>
        <h3>Maak kennis met ons team:</h3>
        <ul>
        <% team.forEach(dino => { %>
            <li>
                <%= dino %>
            </li>
            <% }); %>
            </ul>
        <% } %>
</body>
</html>